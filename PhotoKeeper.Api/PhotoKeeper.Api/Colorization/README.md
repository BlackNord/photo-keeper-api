Installation of the needed packages using comand `pip`:

`pip install numpy`
`pip install opencv-python`

Building of Python script using pyinstaller :

`pip install pyinstaller`
`pyinstaller --onefile Colorization.py`